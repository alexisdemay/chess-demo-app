package fr.chess.demo.enums;

/**
 * The enum Chess engine status enum.
 */
public enum ChessEngineStatusEnum {
    /**
     * Start chess engine status enum.
     */
    START,
    /**
     * Stop chess engine status enum.
     */
    STOP
}
